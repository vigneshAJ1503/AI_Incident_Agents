# aiops backend

Python package for the AI Incident Agents platform (API, orchestrator, agents, MCP client). See the repository [MASTER_PLAN.md](../MASTER_PLAN.md).

```bash
uv sync
uv run aiops --help
uv run pytest
```
