# order-service. Fault flags live in the ConfigMap (PR-017 changes them to inject incidents).
apiVersion: v1
kind: ConfigMap
metadata:
  name: order-service-config
  labels: { app: order-service }
data:
  APP_VERSION: "v2.3.0"
  TEAM: "commerce"
  ENVIRONMENT: "production"
  DB_POOL_SIZE: "10"
  DB_TIMEOUT_MS: "5000"
  UPSTREAM_TIMEOUT_MS: "3000"
  LEAK_KB_PER_REQUEST: "0"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service
  labels: { app: order-service, team: commerce }
spec:
  replicas: 1
  selector: { matchLabels: { app: order-service } }
  template:
    metadata:
      labels: { app: order-service, team: commerce }
    spec:
      containers:
        - name: app
          image: aiops/sample-service:0.1.0
          imagePullPolicy: IfNotPresent
          env:
            - { name: SERVICE_NAME, value: order-service }
            - name: POD_NAMESPACE
              valueFrom: { fieldRef: { fieldPath: metadata.namespace } }
            - name: DATABASE_URL
              valueFrom: { secretKeyRef: { name: app-db, key: DATABASE_URL } }
          envFrom: [{ configMapRef: { name: order-service-config } }]
          ports: [{ name: http, containerPort: 8080 }]
          readinessProbe:
            httpGet: { path: /ready, port: http }
            periodSeconds: 5
          livenessProbe:
            httpGet: { path: /health, port: http }
            periodSeconds: 10
            failureThreshold: 6
          resources:
            requests: { memory: 64Mi, cpu: 25m }
            limits: { memory: 192Mi }
          securityContext:
            runAsNonRoot: true
            allowPrivilegeEscalation: false
---
apiVersion: v1
kind: Service
metadata:
  name: order-service
  labels: { app: order-service, team: commerce }
  annotations: { prometheus.io/scrape: "true", prometheus.io/port: "8080" }
spec:
  type: NodePort
  selector: { app: order-service }
  ports: [{ name: http, port: 8080, targetPort: http, nodePort: 30082 }]
