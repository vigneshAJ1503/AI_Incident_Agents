# Cluster-side metrics (PR-020): kube-state-metrics, scraped by the compose Prometheus
# through NodePort 30080 on the Minikube node (172.21.0.100).
#   make monitoring-up     (= kubectl --context aiops apply -k deploy/k8s/monitoring)
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - kube-state-metrics.yaml
